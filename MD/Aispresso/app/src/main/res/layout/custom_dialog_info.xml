<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/custom_container"
    android:padding="32dp">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/constDialog"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="30dp"
            android:layout_height="30dp"
            app:layout_constraintTop_toTopOf="@+id/constDialog"
            app:layout_constraintStart_toStartOf="@+id/constDialog"
            android:src="@drawable/ic_back"/>

        <TextView
            android:id="@+id/message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:lineSpacingExtra="5dp"
            android:text="@string/const_lorem_ipsum"
            android:textColor="@color/secondary_color"
            android:textSize="18sp"
            android:gravity="center"
            android:layout_marginVertical="10dp"
            app:layout_constraintEnd_toEndOf="@+id/constDialog"
            app:layout_constraintStart_toStartOf="@+id/constDialog"
            app:layout_constraintTop_toBottomOf="@+id/iv_back"
            app:layout_constraintVertical_chainStyle="packed" />

    </androidx.constraintlayout.widget.ConstraintLayout>




    </ScrollView>



</androidx.constraintlayout.widget.ConstraintLayout>